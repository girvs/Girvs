# Girvs 框架升级说明

## 1. 文档范围

本文基于 `dev` 与 `girvs_aspire` 两个分支的最终代码差异整理，面向需要从原 `dev` 版本升级到 `girvs_aspire` 版本的框架使用方。

比较基线：

| 项目 | 值 |
|---|---|
| 原分支 | `dev`（`893ca86`） |
| 目标分支 | `girvs_aspire`（`75de11a`） |
| 共同基点 | `81e2412` |
| 提交差异 | 目标分支新增 124 个提交 |
| 文件差异 | 253 个文件，新增 15,747 行、删除 1,351 行 |
| SDK 与目标框架 | .NET SDK 10.0；通用库支持 `net8.0;net9.0;net10.0`，Aspire 相关项目仅支持 `net10.0` |
| 框架包版本 | `10.0.0-rc.1`，与原 `dev` 分支一致 |

> 本文以目标分支最终代码为准。分支内早期设计文档中“移除 Consul”等阶段性方案已经过调整；最终版本保留 Consul 兼容能力。

## 2. 升级概览

本次升级不是单纯的依赖版本更新，主要变化是基础设施配置、身份上下文和微服务运行方式的系统性重构：

1. 新增 `Girvs.Aspire` 与 `Girvs.Aspire.Hosting`，支持 Aspire 服务发现、健康检查、OpenTelemetry、HttpClient 弹性策略和 AppHost 编排。
2. 数据库、缓存、事件总线和 SignalR 统一通过顶层 `Resources` 声明基础设施，由各模块使用 `ConnectionRef` 引用。
3. 新增 `Girvs.Gateway`，统一支持 Aspire、Consul、Kubernetes 三种服务发现源，并提供可选的 Redis 流程防护。
4. Refit 将“地址类型”和“发现提供者”分离，支持静态地址、Aspire 服务发现和 Consul 服务发现。
5. 身份模型从 `IGirvsClaimManager` / `GirvsIdentityClaim` 迁移到标准 `ClaimsPrincipal`，并支持身份跨 HTTP、后台任务和 EventBus 执行流传播。
6. 核心库内置 AntiJump 实现被删除，严格顺序调用控制改由网关流程防护承担。
7. 配置文件不再在每次启动时回写；文件存在时保持原内容，避免运行过程覆盖手写配置。

## 3. 新增模块

### 3.1 Girvs.Aspire

服务端 Aspire 集成模块，主要提供：

- `Microsoft.Extensions.ServiceDiscovery` 服务发现；
- HttpClient 标准弹性策略；
- OpenTelemetry Trace、Metric 和 OTLP 导出；
- Serilog 日志到 Aspire Dashboard 的 OTLP 桥接；
- `/health` 与 `/alive` 健康检查端点；
- CAP OpenTelemetry 集成。

业务服务需要引用 `Girvs.Aspire`，并在根模块中声明依赖：

```csharp
[DependsOn(typeof(AspireModule))]
public sealed class AppModule;
```

`OTEL_EXPORTER_OTLP_ENDPOINT` 存在时才启用 OTLP 导出，因此服务脱离 AppHost 独立运行时不会强制依赖 Dashboard。

### 3.2 Girvs.Aspire.Hosting

AppHost 编排扩展模块，仅用于 Aspire AppHost 项目。核心 API 如下：

```csharp
var redis = builder.AddRedis("platform-redis")
    .AsGirvsResource();

var mysql = builder.AddMySql("mysql-server")
    .AddDatabase("order-mysql", databaseName: "order")
    .AsGirvsResource();

var service = builder.AddGirvsProject<Projects.Order_Service>();
```

升级时需注意：

- 必须先添加并调用 `AsGirvsResource()` 登记基础设施，再调用 `AddGirvsProject()`；
- `AddGirvsProject<TProject>()` 会按项目元数据名称生成统一的小写连字符服务名；
- Run 模式会将登记资源写入 AppHost 下的 `girvs.shared.json`，并通过 `GIRVS_SHARED_CONFIG` 注入服务；
- Publish 模式只注入约定挂载路径 `/girvs-config/girvs.shared.json`，生产资源配置应由 ConfigMap/Secret 或现有运维体系提供；
- `girvs.shared.json` 已存在时只更新 `Resources` 节点，其他手写配置保持不变；
- 自定义资源可实现 `IGirvsResourceSettingsProvider`，框架会通过 TypeFinder 自动发现，无需手工注册。

详细用法见 [AppHost 接入指南](docs/aspire/apphost-guide.md) 和 [Aspire 参照实现](samples/README.md)。

### 3.3 Girvs.Gateway

新网关包统一封装 YARP 路由生成和服务发现，支持：

| 部署方式 | `GatewayDiscoveryType` | 行为 |
|---|---|---|
| 本地 Aspire AppHost | `Aspire` | 读取 AppHost 注入的 `services:{name}` 端点 |
| 传统部署 | `Consul` | 从 Consul Agent Service 生成路由 |
| Kubernetes | `Kubernetes` | relist + watch Service，动态刷新路由 |

基本接入方式：

```csharp
services.AddGirvsGateway(
    new GatewayDiscoveryConfig
    {
        DiscoveryType = GatewayDiscoveryType.Kubernetes
    },
    configuration);

var source = app.ApplicationServices
    .GetRequiredService<IGatewayServiceDiscoverySource>();
await source.StartAsync(CancellationToken.None);

app.MapReverseProxy();
```

`StartAsync` 不会由 DI 自动调用，网关启动时必须显式执行。Kubernetes 模式使用集群范围的 Service list/watch，需要为 Pod 配置相应 `ClusterRole` 与 `ClusterRoleBinding`。完整说明见 [Girvs.Gateway README](Girvs.Gateway/README.md)。

## 4. 基础设施配置迁移

### 4.1 统一 Resources 模型

`AppSettings` 新增顶层 `Resources`：

```json
{
  "Resources": {
    "platform-redis": {
      "Type": "redis",
      "Settings": {
        "Endpoints": "redis:6379",
        "Ssl": "false"
      }
    },
    "order-mysql": {
      "Type": "mysql",
      "Settings": {
        "Host": "mysql",
        "Port": "3306",
        "Database": "order",
        "UserName": "app",
        "Password": "***"
      }
    },
    "platform-rabbitmq": {
      "Type": "rabbitmq",
      "Settings": {
        "Host": "rabbitmq",
        "Port": "5672",
        "UserName": "guest",
        "Password": "***",
        "VirtualHost": "/"
      }
    }
  }
}
```

配置优先级为：服务自身配置高于 `GIRVS_SHARED_CONFIG` 指向的共享配置。生产环境不要把真实密码提交到仓库。

### 4.2 Cache 配置

原配置中的 `DistributedCacheType` 与直接 `ConnectionString` 被移除，改为 `ConnectionRef`：

```json
{
  "ModuleConfigurations": {
    "CacheConfig": {
      "EnableCaching": true,
      "DistributedCacheConfig": {
        "Enabled": true,
        "ConnectionRef": "platform-redis",
        "DefaultDatabase": 0,
        "InstanceName": "order-service"
      }
    }
  }
}
```

当前实现接受 `Resources` 中 `Type` 为 `redis` 或 `redis-synchronized-memory` 的资源。原 `DistributedCacheType` 类型已删除，业务代码若直接引用该枚举必须同步修改。

### 4.3 Entity Framework Core 配置

原有主从连接串字段改为资源引用：

| 原配置 | 新配置 |
|---|---|
| `MasterDataConnectionString` | `ConnectionRef` |
| `ReadDataConnectionString` | `ReadConnectionRefs` |

```json
{
  "ModuleConfigurations": {
    "DbConfig": {
      "DataConnectionConfigs": [
        {
          "Name": "default",
          "ConnectionRef": "order-mysql",
          "ReadConnectionRefs": ["order-mysql-read-1"],
          "EnableAutoMigrate": false
        }
      ]
    }
  }
}
```

连接串由 `DataConnectionConfig` 在启动时从 `Resources` 组装，仍通过原有的 `GetMasterDataConnectionString()` / `GetSecureRandomReadDataConnectionString()` 读取，业务代码无需改动；原 `MasterDataConnectionString`、`ReadDataConnectionString` 属性已被 `ConnectionRef`、`ReadConnectionRefs` 取代。当前 EF Core 资源构建实现要求 `Type=mysql`；升级前需确认原 SQL Server、Oracle 或 SQLite 业务是否有对应适配，不能直接套用 MySQL 配置。

### 4.4 EventBus 配置

原 `EventBusType`、`DbType`、`DbConnectionString`、`RabbitMqConfig`、`KafkaConfig`、`RedisConfig` 被统一替换为持久化资源和传输资源引用：

```json
{
  "ModuleConfigurations": {
    "EventBusConfig": {
      "PersistenceConnectionRef": "eventbus-mysql",
      "TransportConnectionRef": "platform-rabbitmq",
      "ConsumerThreadCount": 1,
      "ProducerThreadCount": 1,
      "SucceedMessageExpiredAfter": 60,
      "FailedMessageExpiredAfter": 1296000
    }
  }
}
```

- 持久化资源支持 `mysql`、`sqlserver`、`sqlite`；
- 传输资源由 `Type` 自动选择 CAP RabbitMQ、Kafka 或 Redis；
- 未配置资源或类型不匹配时会在启动阶段直接报错，应先完成配置迁移再部署。

### 4.5 SignalR 配置

SignalR 新增 `SignalRConfig.RedisConnectionRef`。为空时不启用 Redis Backplane；集群部署时应指向一个 `Type=redis` 的共享资源。

## 5. 身份体系迁移

### 5.1 破坏性 API 变化

以下类型或成员已删除：

- `IGirvsClaimManager`；
- `GirvsIdentityClaim`；
- `GirvsIdentityClaimTypes`；
- `IdentityClaim`；
- `IEngine.ClaimManager`；
- 原 ClaimManager 扩展方法。

替代 API：

| 原用法 | 新用法 |
|---|---|
| `EngineContext.Current.ClaimManager` | `EngineContext.Current.PrincipalAccessor` |
| `claimManager.GetUserId()` | `principal.GetUserId()` |
| `claimManager.GetTenantId()` | `principal.GetTenantId()` |
| `claimManager.IdentityClaim` | `principal` 及其 Claims |
| `GirvsIdentityClaimTypes.*` | `GirvsClaimTypes.*` |
| 临时替换当前身份 | `using (principalAccessor.Change(principal)) { ... }` |

示例：

```csharp
var principal = EngineContext.Current.PrincipalAccessor.Principal;
var userId = principal.GetUserId<Guid>();
var tenantId = principal.GetTenantId();
var identityType = principal.GetIdentityType();
```

`GirvsPrincipalAccessor` 优先读取当前异步执行流内的身份，否则回退到 `HttpContext.User`。因此后台任务、EventBus Handler 和 HTTP 请求可以使用同一套访问方式。

### 5.2 JWT、租户和 EventBus

- JWT 认证改为直接构造标准 `ClaimsPrincipal`；
- 新增 `GirvsTenantClaimsMiddleware`，通过请求头补充注册用户的租户 Claim；
- EventBus 发布时把允许的 Claim 序列化到 `girvs-identity` Header，消费时自动恢复并进入临时身份作用域；
- 身份 Header 最大 8,192 字节，只允许框架定义的 Claim，禁止把任意业务数据塞入身份上下文；
- `GirvsIntegrationEventHandler`、CAP Filter、Quartz Job 和仓储审计逻辑均已改用统一 Principal。

业务系统如果自行实现事件处理基类、认证中间件、审计字段或后台任务身份切换，需要逐项替换旧 ClaimManager 依赖。

## 6. Refit 服务调用迁移

Refit 将接口地址来源改为明确枚举：

```csharp
[RefitService("order-service", RefitServiceAddressType.ServiceDiscovery)]
public interface IOrderService;

[RefitService("partner-api", RefitServiceAddressType.Static)]
public interface IPartnerApi;
```

配置示例：

```json
{
  "ModuleConfigurations": {
    "RefitConfig": {
      "DiscoveryProvider": "Aspire",
      "ConsulAddress": "http://127.0.0.1:8500",
      "ServiceEndpoints": {
        "partner-api": "https://partner.example.com"
      }
    }
  }
}
```

升级规则：

- 内部服务使用 `ServiceDiscovery`，由 `DiscoveryProvider` 在 Aspire 与 Consul 之间选择；
- 第三方或遗留固定地址使用 `Static`，地址必须配置在 `ServiceEndpoints`；
- `RefitServiceAttribute(string, bool)`、`InConsul`、`ServiceAddress`、`ConsulServiceHost` 暂时保留但已标记为 `Obsolete`，应尽快迁移；
- 服务名统一为小写连字符形式，例如程序集 `Sample.ServiceB` 对应 `sample-serviceb`；
- HttpClient 会自动透传当前 Principal 的身份 Claim，并使用框架统一的认证处理器。

## 7. Consul 兼容策略

目标分支最终状态仍保留 `Girvs.Consul`，用于传统非 Aspire/Kubernetes 部署：

- `Girvs.Consul` 继续提供注册、健康检查和服务发现；
- `Girvs.Gateway` 支持 `GatewayDiscoveryType.Consul`；
- `Girvs.Refit` 支持 `RefitDiscoveryProvider.Consul`；
- 服务名统一由 `ServiceNameResolver` 生成，避免 Consul、Aspire、Refit 和网关命名不一致。

Aspire 与 Consul 的服务端接入仍应按部署模式二选一，不要在同一服务中重复注册发现能力。迁移到 Aspire 不是强制下线 Consul，可按环境逐步切换。

## 8. AntiJump 与网关流程防护

核心项目 `Girvs` 中以下 AntiJump 类型已删除：

- `AntiJumpAttribute`；
- `TokenAntiJumpAttribute`；
- `AntiJumpActionFilterAttribute`；
- `AntiJumpLogic`。

如果业务依赖这些类型，升级后会编译失败。新的 `Girvs.Gateway.FlowProtection` 提供基于 Redis Lua 的严格线性流程控制，包括 Ticket、步骤预占、提交、释放、过期和 ProblemDetails 错误响应。

二者并非无条件等价替换：新方案位于网关层，只适合下游接口无法绕过网关的场景。升级前需要重新梳理受保护接口、流程步骤、Redis 高可用和前端 Ticket 保存策略，详细配置见 [Girvs.Gateway README](Girvs.Gateway/README.md#流程防护)。

## 9. 其他行为变化

### 9.1 appsettings 不再自动覆盖

`AppSettingsHelper.SaveAppSettingsAsync/SaveAppSettings` 现在仅在目标文件不存在时创建文件；文件已存在则直接返回。该变化可避免运行时把枚举、共享资源地址或环境配置写回源码目录。

若业务依赖框架自动持久化运行时配置，需要改为显式的配置管理逻辑。

### 9.2 服务名规则统一

- 程序集名：`.` 转换为 `-`，再转小写；
- Aspire 项目元数据名：`_` 转换为 `-`，再转小写。

网关路由、Refit 服务名、Consul 注册名和 AppHost 项目名应统一使用该结果。升级后应检查所有硬编码服务名、路由前缀和监控规则。

### 9.3 依赖升级与安全修复

分支中更新了 AutoMapper、OpenTelemetry、Aspire、CAP、ServiceDiscovery 等依赖，并对部分传递依赖增加显式版本覆盖以修复已知漏洞。升级业务项目时应以目标分支各模块 `.csproj` 为准执行还原，并检查自有包对 `net8.0`、`net9.0`、`net10.0` 的兼容性。

## 10. 推荐升级步骤

1. 锁定 `dev` 版本并创建业务升级分支，记录当前可用构建与测试结果。
2. 更新 Girvs 包或项目引用；使用 Aspire 的服务和 AppHost 统一升级到 `net10.0`。
3. 全局搜索并迁移已删除 API：

   ```bash
   rg "IGirvsClaimManager|GirvsIdentityClaim|GirvsIdentityClaimTypes|ClaimManager|DistributedCacheType|MasterDataConnectionString|ReadDataConnectionString|EventBusType|DbConnectionString|RabbitMqConfig|KafkaConfig|RedisConfig|AntiJump"
   ```

4. 新增顶层 `Resources`，依次迁移 Cache、EF Core、EventBus、SignalR 的 `ConnectionRef`。
5. 将固定外部地址迁入 Refit `ServiceEndpoints`，内部调用改为 `ServiceDiscovery`。
6. 若接入 Aspire，为服务添加 `Girvs.Aspire` 和根模块依赖，再创建 AppHost、登记资源并添加服务项目。
7. 若升级网关，选择 Aspire、Consul 或 Kubernetes 发现源，并确保启动时调用 `IGatewayServiceDiscoverySource.StartAsync`。
8. 迁移身份访问、JWT 自定义逻辑、事件处理器、后台任务和审计逻辑到 `ClaimsPrincipal`。
9. 对原 AntiJump 使用点逐个评估；确需顺序控制时再接入网关流程防护。
10. 完成单元测试、集成测试和目标部署环境验证后再切换生产流量。

## 11. 验证清单

### 构建与单元测试

```bash
dotnet restore Girvs.slnx --nologo
dotnet build Girvs.slnx
dotnet test Girvs.slnx
```

涉及 Aspire、网关、Refit 或身份迁移时，至少额外确认以下测试项目通过：

```bash
dotnet test tests/Girvs.Aspire.Tests/Girvs.Aspire.Tests.csproj
dotnet test tests/Girvs.Aspire.Hosting.Tests/Girvs.Aspire.Hosting.Tests.csproj
dotnet test tests/Girvs.Gateway.Tests/Girvs.Gateway.Tests.csproj
dotnet test tests/Girvs.Refit.Tests/Girvs.Refit.Tests.csproj
dotnet test tests/Girvs.Claims.Tests/Girvs.Claims.Tests.csproj
```

### 运行验证

- `/health`、`/alive` 返回 200；
- Cache 能完成写入与读取；
- EF Core 能连接主库，配置读库时可正常读取；
- CAP 能发布、持久化和消费消息，消费端能恢复用户及租户身份；
- Refit 内部调用能通过选定的发现提供者解析服务；
- 网关能生成路由，服务增删后路由能按发现源预期刷新；
- Aspire Dashboard 能看到 Trace、Metric 和 Serilog 日志；
- 生产共享配置文件已挂载，且未把密钥或真实连接串写入仓库。

## 12. 已知限制与升级风险

| 风险或限制 | 升级建议 |
|---|---|
| Aspire 模块仅支持 `net10.0` | AppHost 和接入 Aspire 的服务统一使用 .NET 10 SDK |
| EF Core 资源连接构建当前只支持 MySQL | 其他数据库先补充适配与测试，再迁移配置 |
| 配置绑定到 Singleton 后是启动快照 | 修改共享资源配置后重启服务 |
| K8s 网关为集群范围 watch | 最小化 ServiceAccount 权限并审查 ClusterRole |
| Aspire 与 Consul 同时接入可能重复注册 | 每个部署环境只选择一种服务端发现模式 |
| AntiJump 已删除且新流程防护位于网关 | 先做业务语义评估，不要机械替换 |
| 旧 Refit API 仅是 Obsolete 兼容 | 在后续版本移除前主动完成迁移 |
| `girvs.shared.json` 会更新 `Resources` | 决定是否提交资源快照，避免无意义 Git 差异 |

## 13. 参考资料

- [Girvs Aspire AppHost 接入指南](docs/aspire/apphost-guide.md)
- [Girvs Aspire 业务升级方案](docs/aspire/升级方案.md)
- [Girvs Aspire 参照实现](samples/README.md)
- [Girvs Gateway 使用说明](Girvs.Gateway/README.md)
- [Kubernetes 网关样例](samples/gateway-k8s/README.md)
